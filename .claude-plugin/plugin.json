{
  "name": "workspace-os",
  "version": "0.19.0",
  "description": "Portable project OS: command-driven tracking (project-init/log/plan) + per-repo shared memory (ingest/memory-lint/memory-search/memory-sync/memory-adopt/tracking-adopt) + sidecar workspace mode (workspace-init) + a PreToolUse guardrail engine (declarative per-repo deny/warn rules) across your repos.",
  "author": { "name": "MDesai31" }
}
