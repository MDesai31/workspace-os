{
  "name": "workspace-os",
  "version": "0.12.0",
  "description": "Portable project OS: command-driven tracking (project-init/log/plan) + per-repo shared memory (ingest/memory-lint/memory-sync/memory-adopt/tracking-adopt) + sidecar workspace mode (workspace-init) + a PreToolUse guardrail engine (declarative per-repo deny/warn rules) across your repos.",
  "author": { "name": "MDesai31" }
}
